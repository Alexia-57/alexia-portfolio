<div class="row my-5">
  <div class="col-12">
    <div class="container">
      <div id="projects" class="card-block">
        <h2>Projects</h2>
        <div class="cards">
          <% data.projects.each do |project| %>
            <a href="<%= project.url %>" target="_blank" class="card-product">
              <%= image_tag project.image, alt: "project image" %>
              <div class="card-product-infos">
                <h4><%= project.name %><i class="fas fa-external-link-alt"></i></h4>
                <p><%= project.description %></p>
                <p class="tech"><%= project.tech %></p>
              </div>
            </a>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
